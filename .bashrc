#
# ~/.bashrc
#

alias killdwm="~/.scripts/killdwm.sh"
alias dotfiles="git --git-dir=$HOME/.dotfiles --work-tree=$HOME"
alias c="clear"

# If not running interactively, don't do anything
#[[ $- != *i* ]] && return

alias ls='ls --color=auto'
#PS1='[\u@\h \W]\$ '
